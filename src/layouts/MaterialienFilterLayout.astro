---
import type { CollectionEntry } from "astro:content";
import BaseLayout from "./BaseLayout.astro";
import Breadcrumbs from "../components/Breadcrumbs.astro";
import MaterialKarte from "../components/MaterialKarte.astro";

interface Props {
  title: string;
  filterLabel: string;
  entries: CollectionEntry<"materialien">[];
}

const { title, filterLabel, entries } = Astro.props;
---

<BaseLayout
  title={`${title} – Hilfreiches Material`}
  description={`Kostenlose Materialien, gefiltert nach: ${filterLabel}.`}
>
  <div class="container">
    <Breadcrumbs
      items={[
        { label: "Start", href: "/" },
        { label: "Hilfreiches Material", href: "/materialien/" },
        { label: title },
      ]}
    />
  </div>

  <section class="section">
    <div class="container">
      <h1>{title}</h1>
      <p class="lead">
        <a href="/materialien/">Zurück zur vollständigen Übersicht und zu weiteren Filtern</a>
      </p>

      <h2 class="visually-hidden">Treffer</h2>

      {
        entries.length === 0 ? (
          <div class="notice">
            <p>Für diesen Filter gibt es aktuell noch keine Einträge.</p>
          </div>
        ) : (
          <ul class="material-grid">
            {entries.map((entry) => (
              <li>
                <MaterialKarte entry={entry} />
              </li>
            ))}
          </ul>
        )
      }
    </div>
  </section>
</BaseLayout>

<style>
  .lead {
    max-width: var(--measure);
    margin-bottom: var(--space-lg);
  }

  .material-grid {
    list-style: none;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(17rem, 1fr));
    gap: var(--space-md);
  }
</style>
