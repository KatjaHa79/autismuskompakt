---
interface Props {
  titel?: string;
}

const { titel } = Astro.props;
---

<div class="notice" role="note">
  {titel && <p class="notice__titel">{titel}</p>}
  <slot />
</div>

<style>
  .notice__titel {
    font-weight: 700;
    margin-bottom: var(--space-2xs);
  }
</style>
